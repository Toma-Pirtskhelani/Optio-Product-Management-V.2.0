.DS_Store
*.swp
.env
.venv/
__pycache__/
node_modules/

# evidence/raw/ IS committed on purpose — saved source pages are the evidence.
# If a single capture exceeds ~50MB, store it externally and record the pointer
# in evidence/fetch-log.md rather than ignoring it silently.
